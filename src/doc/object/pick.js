const _ = require("lodash");

/*
 * 接收 2 个参数：'对象'，'属性路径数组'；
 * 返回一个新的对象，'返回对象'的属性为从'对象'上根据'属性路径数组'拿取的属性；
 * --------------------------------------------------
 */
const obj01 = { x: 1, y: 2, z: 3 };
/* { x: 1, y: 2 } */
console.log(_.pick(obj01, ["x", "y"]));
