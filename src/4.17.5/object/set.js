const _ = require("lodash");

/*
 * 接受 3 个参数：'对象'，'属性路径'，'属性值'；
 * 设置'对象'在'属性路径'的属性的值为'属性值'；
 * 如果路径不存在，会被自动创建；
 * --------------------------------------------------
 */
const obj01 = {};
_.set(obj01, "x[0].y.z", "tom");
/* { x: [{ y: { z: "tom" } }] } */
console.log(JSON.stringify(obj01));
