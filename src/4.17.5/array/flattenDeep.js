const _ = require("lodash");

/*
 * 接收 1 个参数：'数组'
 * 递归平化'数组'，返回平化后的新数组，'数组'不变；
 * --------------------------------------------------
 */

/*
 * 递归平化，直到最后一层（与 _.flatten 的区别）；
 * --------------------------------------------------
 */
/* [ 1, 2, 3, 4, 5 ] */
console.log(_.flattenDeep([1, [2, [3, [4]]], 5]));
