const _ = require("lodash");

/*
 * 接收 2 个参数：'对象'，'属性路径数组'；
 * 返回一个'数组'，'数组'元素为'对象'在'属性路径'的属性值；
 * --------------------------------------------------
 */

const obj01 = { a: { b: "tom" }, c: [{ x: { age: 18 } }] };
/* [ 'tom', 18 ] */
console.log(_.at(obj01, ["a.b", "c[0].x.age"]));
